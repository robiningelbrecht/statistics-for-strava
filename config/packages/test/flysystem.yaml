flysystem:
    storages:
        default.storage:
            adapter: 'memory'

        gear_maintenance.storage:
            adapter: 'memory'

        file.storage:
            adapter: 'memory'

        database.storage:
            adapter: 'memory'

        build.storage:
            adapter: 'memory'

        build_html.storage:
            adapter: 'memory'

        build_api.storage:
            adapter: 'memory'