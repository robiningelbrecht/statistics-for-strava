services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./index.html:/srv/index.html:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:2019/metrics"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped
