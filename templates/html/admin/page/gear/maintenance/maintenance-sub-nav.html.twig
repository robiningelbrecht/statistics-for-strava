{% autoescape false %}
    <div class="inline-flex items-center gap-1 self-start mb-4 rounded-lg border border-gray-200 bg-white p-1 text-xs font-medium shadow-xs">
        <a href="{{ relativeUrl(path('admin_manage_gear_maintenance_logs_overview')) }}"
           class="rounded-md px-3 py-1.5 {{ activeSubNav == 'log' ? 'bg-primary-soft text-primary' : 'text-gray-500 transition-colors hover:text-gray-700' }}">
            {{ "Log"|trans }}
        </a>
        <span class="inline-flex items-center gap-1.5 rounded-md px-3 py-1.5 text-gray-400 cursor-not-allowed"
              title="Coming soon"
              aria-disabled="true">
            {{ "Config"|trans }}
            <span class="rounded-full bg-gray-100 px-1.5 py-0.5 text-xxs font-semibold uppercase tracking-wide text-gray-400">
                Soon
            </span>
        </span>
    </div>
{% endautoescape %}
